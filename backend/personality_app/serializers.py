# Serializers for personality prediction API
# In your case, you're directly returning AI model results as JSON
# so complex serializers aren't needed - Django REST Framework
# handles the JSON conversion automatically